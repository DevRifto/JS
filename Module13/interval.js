setTimeout(); // set timeout delay how late you want to show the item.
clearTimeout();// it will print the setTimeout value but it will cancel the timer !
setInterval();// set Interval loops the item and its also can be delayed like setTimeout.
clearInterval();// clearInterval stop the loop of setInterval() by giving setInterval and id
// x++ vs ++x num = 0; (num++)=1