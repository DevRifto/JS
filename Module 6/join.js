// Make any arrays in one line word :
const words = ['a', 'b', 'c', 'd', 'e'];
let oneLine = words.join('-'); // Join usaly join the string but it will add symbols you give them (empty join will add it one line without spaces)
console.log(oneLine);