// if you want to show the 'cat' word :
const words = 'Cat is a domistic animal'
// if you want to show the cat word use slice :
// You can also use (-)
let catWord = words.slice(0,3); // the number are like index
console.log(catWord);