// ... this is 3 dots which copy value frome an array and past the value , not call the array it will paste the same value in another array or variable.
// const num = [1, 2, 3, 4, 5];
// const max = Math.max(num); // it will say Nan
// const max = Math.max(...num); // it will past the array value;
// console.log(max);
