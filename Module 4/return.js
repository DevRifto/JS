// if you want to store a funtion in a variable return will need to:
// function userName(firstName, secondName){
//     let fullName = 'Welcome ' + firstName + secondName;
// }
// let newName = userName('Rifat','o');
// console.log(newName);// arguments

// The outPut will be undefined. beacuse you have sent the arguments but if you set the function name in a variable but you don't use return so it will not return anything so the variable will be undefined {EMPTY};

// and you just type return; it will also be undefined because you have to return something ! 

// return also work like a break statement (if you call return the rest of the valu will not print it will be end of the function)

//Solution :
// function userName(firstName, secondName){
//     let fullName = 'Welcome ' + firstName + secondName;
//     return fullName;
// }
// let newName = userName('Rifat','o');
// console.log(newName);
