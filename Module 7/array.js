// There are many tipe that can be used to store the values :
// 1. variable
// 2. function
// 3. object
// 4. array
// 5. string
// 7. boolean

// if you serch typeof it will say that it's string type , Object type , boolean , number type,
// but in array its wil say Object. so to fixed that you can use array.isArray(arrays name) if its true its is an array but if it's false it's not an array.